let count = 0; // Глобальная переменная

// Вызываем функцию, которая увеличивает count
function incrementCount() {
  count++;
  console.log(count);
}

incrementCount();
incrementCount();
incrementCount();
incrementCount();
